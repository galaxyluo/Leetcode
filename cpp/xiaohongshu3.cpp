#include <bits/stdc++.h>
using namespace std;

class Solution{
public:
    int maxTime() {

    }
    map<int, int> str2time(vector<string>& times) {
        for (string time: times) {
            string tmp;
            int start, end;
            for (int i = 0; i < time.size(); i++) {
                if (time[i] == ':')
                    continue;
                else if (time[i] == '-') {
                    
                }
            }
        }
    }
};

int main() {
}